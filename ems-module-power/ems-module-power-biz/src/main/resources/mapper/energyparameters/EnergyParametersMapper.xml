<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bitlinks.ems.module.power.dal.mysql.energyparameters.EnergyParametersMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.bitlinks.cn/MyBatis/x-plugins/
     -->

    <delete id="deleteByEnergyId">
        DELETE FROM ems_energy_parameters
        WHERE energy_id = #{energyId}
    </delete>

    <delete id="deleteByEnergyIds">
        DELETE FROM ems_energy_parameters
        WHERE energy_id IN
        <foreach collection="energyIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectByEnergyId" resultType="EnergyParametersDO">
        SELECT *
        FROM ems_energy_parameters
        WHERE energy_id = #{energyId}
          AND deleted = 0
    </select>

    <select id="selectListByEnergyIds" resultType="EnergyParametersDO">
        SELECT *
        FROM ems_energy_parameters
        WHERE energy_id IN
        <foreach collection="energyIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND deleted = 0
    </select>
</mapper>