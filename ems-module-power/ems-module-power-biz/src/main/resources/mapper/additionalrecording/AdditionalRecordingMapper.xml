<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bitlinks.ems.module.power.dal.mysql.additionalrecording.AdditionalRecordingMapper">
    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.bitlinks.cn/MyBatis/x-plugins/
     -->


    <select id="countByVoucherId" resultType="java.lang.Integer">
        select count(0)
        from ems_additional_recording
        where voucher_id = #{voucherId}
    </select>

    <select id="countByVoucherIds" resultType="java.lang.String">
        select ev.code
        from ems_voucher ev
                 left join ems_additional_recording ear on ev.id = ear.voucher_id
        <where>
            ear.voucher_id in
            <foreach collection="voucherIds" separator="," item="voucherId" open="(" close=")">
                #{voucherId}
            </foreach>
        </where>
    </select>

    <select id="selectStandingbookIdByVoucherIds" resultType="java.lang.Long">
        SELECT DISTINCT standingbook_id
        FROM ems_additional_recording
        WHERE voucher_id = #{voucherId}
    </select>
</mapper>