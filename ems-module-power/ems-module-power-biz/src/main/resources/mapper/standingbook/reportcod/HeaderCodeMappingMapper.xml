<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bitlinks.ems.module.power.dal.mysql.standingbook.reportcod.HeaderCodeMappingMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.bitlinks.cn/MyBatis/x-plugins/
     -->


    <select id="selectByHeaderCode" resultType="cn.bitlinks.ems.module.power.controller.admin.additionalrecording.vo.HeaderCodeMappingVO">
        SELECT
            hcm.header_code,
            hcm.header,
            hcm.`code`,
            sa.standingbook_id
        FROM
            `ems_header_code_mapping` hcm
                LEFT JOIN ( SELECT standingbook_id, `value` FROM power_standingbook_attribute WHERE CODE = 'measuringInstrumentId' AND standingbook_id IS NOT NULL ) sa ON hcm.`code` = sa.`value`
        WHERE
            hcm.header IN
        <foreach collection="headers" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>


    </select>
</mapper>