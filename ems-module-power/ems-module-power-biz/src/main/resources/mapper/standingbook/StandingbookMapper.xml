<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bitlinks.ems.module.power.dal.mysql.standingbook.StandingbookMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.bitlinks.cn/MyBatis/x-plugins/
     -->

    <select id="selectStandingbookIdByCondition" resultType="java.lang.Long">
        select id from(
        select * from(
        SELECT
        id
        <if test="labelInfoConditions!= null and labelInfoConditions.size > 0">
            ,
            <foreach collection="labelInfoConditions.keySet()" item="key" separator=",">
                JSON_EXTRACT(label_info, concat('$.', #{key})) AS ${key}
            </foreach>
        </if>
        FROM
        power_standingbook
        where deleted = 0
        <if test="typeId!= null">
            AND type_id = #{typeId}
        </if>
        <if test="createTimeArr != null and createTimeArr.size() > 0">
            AND create_time BETWEEN #{createTimeArr[0]} AND #{createTimeArr[1]}
        </if>
        ) as r
        where 1=1
            <if test="labelInfoConditions!= null and labelInfoConditions.size > 0">
                and
                <foreach collection="labelInfoConditions" item="valueList" index="key" separator=" and ">
                    (
                    <foreach collection="valueList" item="itemValue" separator=" and ">
                        ${key} LIKE CONCAT('%', #{itemValue}, '%')
                    </foreach>
                    )
                </foreach>
            </if>
        ) AS rr
    </select>
</mapper>
