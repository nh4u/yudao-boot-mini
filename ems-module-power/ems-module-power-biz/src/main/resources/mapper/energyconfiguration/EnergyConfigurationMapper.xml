<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bitlinks.ems.module.power.dal.mysql.energyconfiguration.EnergyConfigurationMapper">
    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.bitlinks.cn/MyBatis/x-plugins/
     -->


    <select id="getByEnergyClassify"
            resultType="cn.bitlinks.ems.module.power.dal.dataobject.energyconfiguration.EnergyConfigurationDO">
        SELECT t.id,
               t.group_id,
               t.CODE,
               t.energy_classify,
               t.energy_icon,
               t.factor,
               t.create_time,
               t.update_time,
               t.creator,
               t.updater,
               t.deleted,
               IF(t1.unit is not null, CONCAT(t.energy_name, '(', t1.unit, ')'), t.energy_name) AS energy_name
        FROM ems_energy_configuration t
                 LEFT JOIN ems_energy_parameters t1 ON (t1.energy_id = t.id AND t1.deleted = 0 and t1.`usage` = 1)
        WHERE t.deleted = 0
        <if test="energyIds != null and energyIds.size() != 0">
            and t.id in
            <foreach collection="energyIds" item="energyId" separator="," close=")" open="(">
                #{energyId}
            </foreach>
        </if>
        <if test="energyClassify != null">
            and t.energy_classify = #{energyClassify}
        </if>
    </select>
</mapper>